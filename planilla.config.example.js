/*
  Copia este archivo a planilla.config.js (NO subir a Git).
  Rellena los valores reales y carga este archivo con una etiqueta <script src="planilla.config.js"></script>
*/
window.PLANILLA_CONFIG = {
  // Si prefieres que el frontend consulte Supabase directamente, pega aquí la anon key (no recomendado para datos sensibles):
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'pk.your_anon_key_here',

  // Recomendado: configura un endpoint seguro (ver server.js) y pon su URL aquí.
  ENDPOINT_PLANILLA: '', // e.g. 'https://lirujhan.lat/api/planilla'

  // Logo que aparecerá en el PDF (opcional)
  LOGO_URL: ''
};
